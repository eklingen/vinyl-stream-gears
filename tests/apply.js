
import test from 'ava'
import { src } from 'vinyl-fs'

const apply = require('../modules/apply')

test.todo('should do nothing if `cb` is not a function')
test.todo('should apply `cb` in the context of the stream')
