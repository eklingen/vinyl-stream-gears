import test from 'ava'

const filter = require('../modules/filter')
const apply = require('../modules/apply')

test.todo('should do nothing when argument is not an array with one or more functions')
test.todo('should filter files based on the result from the functions in the filter array')
