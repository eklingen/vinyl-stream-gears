import test from 'ava'

const log = require('../modules/log')
const apply = require('../modules/apply')

test.todo('should log filenames and paths to the console')
test.todo('should also log contents if `options.showContents` is true')
